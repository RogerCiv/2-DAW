{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
 <a href="{{ path('app_game_new') }}">New Game</a>

  {% for game in invitations %}
    Fueiste invitado a una partida por {{ game.player1.username}} el dia {{game.gameDateTime|date('Y-m-d') }}
    <br>
    <a href="game/accept/{{game.id}}" > Jugar Ahora</a>
  {% endfor %}

    {% for game in games %}
   Aqui tienes partida pendiente con  {{ game.player1.username}} el dia {{game.gameDateTime|date('Y-m-d') }}
    <br>
    <a href="game/accept/{{game.id}}" > Jugar Ahora</a>
  {% endfor %}
</div>
{% endblock %}
