{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<br><br>
{% set topWinner =0 %}
{% set userWinner =0 %}

{% set topGamer =0 %}
{% set userGamer =0 %}

nombre usuario - jugadas - ganadas - perdidas<br>
{% for user in users %}
    {% if topWinner < wins[user.id] %}
        {% set topWinner = wins[user.id] %}
        {% set userWinner = user %}
    {% endif %}
    
    {% if topGamer < user.games|length  %}
        {% set topGamer = user.games|length %}
        {% set userGamer = user %}
    {% endif %}
    {{ user.username }} - {{user.games|length }} - {{wins[user.id]}} - {{user.games|length - wins[user.id]}}<br>
{% endfor %}
<br>
El jugador que mas ha ganado es {{userWinner.username}}<br>
El jugador que mas ha jugado es {{userGamer.username}}
{% endblock %}
