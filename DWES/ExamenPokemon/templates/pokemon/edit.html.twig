{% extends 'base.html.twig' %}

{% block title %}Edit Pokemon{% endblock %}

{% block body %}
    <h1>Edit Pokemon</h1>

    {{ include('pokemon/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_pokemon_index') }}" class="btn btn-dark text-ligth-emphasis mt-2">back to list</a>

    {{ include('pokemon/_delete_form.html.twig') }}
{% endblock %}
